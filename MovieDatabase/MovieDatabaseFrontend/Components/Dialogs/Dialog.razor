@* <Modal @ref="modal" Opening="OnOpening" Closing="OnClosing">
    <ModalContent Centered>
        <ModalHeader>
            <ModalTitle>
                @Title
            </ModalTitle>
        </ModalHeader>
        <ModalBody>
            @Content
        </ModalBody>
        <ModalFooter>
            @if (ButtonMode == ButtonMode.AbortSave)
            {
                <Button Color="Color.Secondary" Clicked="OnAbort">Abbrechen</Button>
                <Button Color="Color.Primary" Clicked="OnSave">Speichern</Button>
            }
            else
            {
                <Button Color="Color.Primary" Clicked="OnClose">Schließen</Button>
            }
        </ModalFooter>
    </ModalContent>
</Modal> *@

@code
{
    // [Parameter] public RenderFragment? Content { get; set; }
    // [Parameter] public required string Title { get; set; }
    // [Parameter] public ButtonMode ButtonMode { get; set; } = ButtonMode.Close;

    // private Modal modal = null!;

    // private Task OnOpening(ModalOpeningEventArgs e)
    // {
    //     e.Cancel = selectedMovie is null;
    //     return Task.CompletedTask;
    // }

    // private Task OnClosing(ModalClosingEventArgs e)
    // {
    //     e.Cancel = cancelClose;
    //     return Task.CompletedTask;
    // }

    // private Task OnAbort()
    // {

    // }

    // private Task OnSave()
    // {

    // }

    // private Task OnClose()
    // {

    // }

    // public Task Show()
    // {
    //     return modal.Show();
    // }
}